import { FormatedOrderPipe } from './formated-order.pipe';

describe('FormatedOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatedOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
